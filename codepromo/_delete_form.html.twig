<form method="post" action="{{ path('codepromo_delete', {'id': codepromo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ codepromo.id) }}">
    <button class="btn">Delete</button>
</form>
